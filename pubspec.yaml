name: pyroid_flutter_clone
description: "Flutter ile Gelişmiş Python IDE"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Chaquopy'yi daha eski ve kararlı bir sürüme (10.x.x serisine) çekiyoruz
  chaquopy: ^10.1.1
  code_text_field: ^1.1.0
  flutter_highlight: ^0.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  
